import styled from "styled-components";

export const CloseButton = styled.img`
	width: 25px;
	position: absolute;
	top: 0;
	right: 0;
	padding: 0;
	margin: 12px;
	cursor: pointer;
	background: clear;
`;
